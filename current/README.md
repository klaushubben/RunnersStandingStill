# Current

Current canonical project files.

- `source/` contains the reviewed unminified renderer source.
- `artifacts/` contains packaged/minified files used by contract and site staging.
- `contract-preview/` contains the local deterministic preview harness from the source project.

The minified artifact was copied from:

```text
/Users/nickhubben/Sandbox/GenArtContract/artifacts/runners-standing-still/sketch.runners-standing-still.min.js
```

The unminified renderer was copied from:

```text
/Users/nickhubben/Sandbox/TextureMelt/Apr5_staticmelt/contract-deterministic-renderer/sketch.runners-standing-still.contract.js
```
